# 《黄昏居酒屋》代码审查问题清单

本文件固定保存在 `docs/代码审查问题清单.md`，是代码审查问题、优先级、状态和验收条件的唯一清单。修复问题时更新本文件，不再创建平行清单。

状态：`待处理`、`处理中`、`部分完成`、`已解决`。优先级：`P0` 发布阻断、`P1` 核心流程或安全高风险、`P2` 明显质量/性能问题、`P3` 一般工程债。

## 当前处理顺序

| ID | 优先级 | 问题 | 状态 | 验收条件 |
|---|---:|---|---|---|
| SEC-001 | P0 | 桌面作者 Key 明文生成、打包和玩家 Key 明文落盘 | 已解决 | 无作者 Key 打包命令/文件；Electron 不再持久化玩家 Key；启动时清除旧 `config.json` Key；仓库和构建链无作者 Key 回退 |
| SEC-002 | P1 | Vercel BYOK 使用进程全局状态，跨函数失效且无用户隔离 | 已解决 | Key 仅在前端内存；每次请求显式携带；Express/Vercel/provider 全链路 request-scoped；无 `/api/settings/api-key` |
| SEC-003 | P1 | NPC 公网接口信任客户端角色档案，缺少游戏级鉴权/速率限制 | 部分完成 | 已移除作者额度滥用面；仍需服务端 canonical 角色资料、字段上限及按部署需求决定限流策略 |
| CONTENT-001 | P1 | 阿相 W1_D3 使用旧 `mixing` schema，调酒结果不可达 | 待处理 | 三个旧节点迁移为正式 schema；日程端到端测试可进入 success/fail |
| CONTENT-002 | P1 | 阿相七个 `diary_note` 为对象，运行时要求字符串 | 待处理 | normalizer/内容修正统一为字符串；内容校验拒绝错误类型；结果节点渲染不崩溃 |
| FLOW-001 | P1 | 闲聊总取首节点，终止选项会循环，后续内容不可达 | 待处理 | 统一条件求值；已消费策略明确；终止 option 能退出；多节点可达性测试通过 |
| LLM-001 | P1 | 同轮玩家/NPC transcript 使用旧闭包反复覆盖 | 待处理 | reducer 原子批量追加；玩家输入和所有回复完整保留 |
| LLM-002 | P1 | LLM 请求离场、换客或读档后仍可污染新状态 | 待处理 | AbortController + session/visit generation；提交前复核；竞态测试通过 |
| FLOW-002 | P1 | W1_D5 后进入无客人、无终局出口的软锁 | 待处理 | 显式 `gameComplete/credits` 或正式循环；完整日程 smoke test 到达终局 |
| SAVE-001 | P1 | 损坏/未来版本存档在 IndexedDB 回调抛错后 Promise 永久 pending | 待处理 | 回调显式 reject；坏档隔离且可删除；读档 UI 不挂死 |
| DESKTOP-001 | P1 | 固定回环 API 缺少每次启动 capability 与 Host/Origin 防护 | 待处理 | 随机能力令牌；严格 Host/Origin；非 Electron 请求无法修改/调用桌面服务 |
| DEV-001 | P2 | 一键启动器选择动态后端端口但 Vite proxy 仍指向 3001 | 已解决 | 两份启动器均注入 `VITE_BACKEND_TARGET`；静态回归测试覆盖动态目标传递 |
| DEV-002 | P1 | Node 环境修复脚本对不存在的候选盘符或环境目录执行 `Join-Path`，导致一键启动中止 | 已解决 | 路径组合和文件检查不依赖 PowerShell 盘符提供程序；完整修复流程在失效 `LOCALAPPDATA` / `SystemRoot` 下通过回归测试 |
| DEV-003 | P1 | 调试跳转复用普通迁移，`intro → intro` / `mainMenu → intro` 触发断言并卡死 | 已解决 | 原子 `DEBUG_JUMP` 同步重定位状态与日期、清空跨日临时状态；跳转后关闭设置层；全部根状态回归测试通过 |
| API-001 | P2 | MiniMax timeout 在响应头后清除，不覆盖慢响应体 | 待处理 | timeout 覆盖 body 读取；响应体大小上限；慢 body 测试返回 504 |
| PERF-001 | P2 | 启动硬等待约 35.97 MiB 图片，生产产物约 183.24 MiB | 待处理 | 首屏只加载必要资源；角色/酒谱懒加载；设定产物和首屏预算 |
| QA-001 | P2 | 构建门禁假绿：内容 schema 盲区、宽松 TS、测试原先未接入脚本 | 部分完成 | 已增加统一 `npm test`；仍需 React 类型、strict、内容运行时 schema 和完整日程测试进入 `check/prebuild` |
| UX-001 | P2 | 文字速度、自动播放、屏幕震动设置未被游戏消费 | 待处理 | 设置接入对应组件并有行为测试，或在功能完成前移除入口 |
| CONTENT-003 | P2 | 文件系统校验器与 Vite loader 使用不同文件发现规则 | 待处理 | 两端消费同一精确 manifest；误命名文件能在构建前失败 |
| BUILD-001 | P2 | Electron 收集不需要的前端/构建生产依赖且 `asar:false` | 待处理 | 最小 runtime manifest；删除未用依赖；产物清单和大小预算检查 |
| BUILD-002 | P3 | Windows `clean` 脚本使用 `rm -rf` | 待处理 | 使用跨平台清理命令并在 Windows CI/本机验证 |
| CONTENT-004 | P3 | 酒谱图片缺失与孤儿资源未纳入校验 | 待处理 | 资源 ID 双向校验；允许 fallback 时显式声明 |
| DEP-001 | P1 | 生产依赖审计仍报告高危/中危漏洞（含 Vite、Express 依赖链） | 待处理 | 逐项确认可利用面并升级到已修复版本；`npm audit --omit=dev` 无 high |

## SEC-001 / SEC-002 修复契约

生产 BYOK 数据流固定为：

```text
玩家输入 MiniMax Key
  -> 当前前端页面内存
  -> Authorization: Bearer <key>
  -> 同源 /api/npc-dialogue
  -> request-scoped handler/provider 参数
  -> https://api.minimaxi.com
```

约束：

- 仅支持 MiniMax；客户端不能选择 provider、model 或 base URL。
- 应用代码不把 Key 写入 JSON body、存档、业务日志、响应、服务端模块状态或磁盘；托管层必须配置 Authorization 脱敏。
- 刷新、关闭或重启后 Key 消失，玩家需要重填。
- 旧桌面 `config.json` 中的 `MINIMAX_API_KEY` 会在启动时被清除。
- `devtools/` 的 live 调试命令仍允许开发者从环境变量显式提供测试 Key；该目录不进入生产前端或 Electron 包，也不是生产回退路径。
- 如果旧桌面包曾内置并分发作者 Key，必须在 MiniMax 后台另行撤销/轮换；源码修复不能回收旧包。

## 本轮验证目标

- 生产代码和打包输入不再包含 `desktop:pack:author`、作者 Key 文件、作者 Key 环境变量或 `process.env.MINIMAX_API_KEY` 回退；安全守卫与迁移测试只保留这些名称作为拒绝规则。
- 缺少 Authorization 的 NPC 请求返回 `401`，且不会调用 MiniMax。
- 并发 Key A/Key B 分别进入对应上游请求，不串 Key。
- API Key 不出现在请求 JSON、响应或状态对象中。
- 旧 Electron 明文配置迁移测试通过。
- `npm run content:check`、`npm run lint`、`npm test`、`npm run build` 全部通过。

## 最近一次验证

日期：2026-07-15。

- `npm run content:check`：通过，8 位客人 / 7 天日程。
- `npm run lint`：通过。
- `npm test`：45/45 通过，包括 Vercel 并发 Key 隔离、环境变量不可回退、缺 Key 拒绝、请求不回显、Electron 旧明文配置清理、打包 fail-closed、启动器容错，以及从全部 15 个根状态执行原子调试跳转测试。
- 生产构建：通过；仍有约 710.09 kB 主 JS chunk 警告，对应 `PERF-001`。
- 生产源码与前端构建产物禁用标记扫描：未发现作者 Key 打包入口、作者 Key 环境变量、Gemini 浏览器注入或旧打包文件名。
- Electron unpacked 打包：通过；实际 `resources/app` 中作者/测试 Key 文件名计数为 0，旧 `server/apiSettings` 与 `@google/genai` 均不存在。应用资源约 262.44 MB，对应 `BUILD-001/PERF-001`。
- `npm audit --omit=dev`：仍报告 3 个 high、5 个 moderate、2 个 low，已登记为 `DEP-001`，本轮未自动执行破坏性升级。
